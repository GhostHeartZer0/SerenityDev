Created At: 2026-07-17T20:37:54Z
Last Updated: 2026-09-02T11:45:00Z
File Path: `file:///F:/SerenityDev/TODO.txt`

=========================================
ACTIVE TODO ITEMS & PRIORITIES
=========================================

[DONE] MCP JSON-RPC Spec Alignment (tools/call & Notifications):
- Aligned `@app.api_route("/mcp", ...)` in `serenitydevserver.py` with JSON-RPC 2.0 and MCP 2024-2026 specification.
- Notifications (`notifications/initialized`, `initialized`, `ping`) return HTTP 200/204 with empty result body or omitted/null id as required by spec.
- Enhanced `tools/call` parameter parsing to handle arguments wrapped in `arguments`, `parameters`, or raw dictionary/JSON string inputs.
- Verified fallback method handler returns spec-compliant `-32601` JSON-RPC error. [Done]

[DONE] Streamline start_native_mcp.py Root CA & Network Binding:
- Bound CA download `SimpleHTTPRequestHandler` cleanly on `0.0.0.0` with `allow_reuse_address = True` on `ReusableHTTPServer`.
- Set MIME content type to `application/x-x509-ca-cert` for immediate Android certificate installation.
- Included all detected local IP addresses and hostname entries in leaf certificate `SubjectAlternativeName` to eliminate TLS handshake SAN mismatch errors for external devices/phones over Wi-Fi.
- Added `*.crt` and `rootCA.crt` to `.gitignore` to prevent certificate tracking in git. [Done]

[DONE] VS Code Client Auth Token Synchronization:
- Updated `serenityHttp` in `src/extension.ts` to attach `Authorization: Bearer <token>` when `process.env.SERENITY_MCP_TOKEN` or configured `serenitydev.mcpToken` is present.
- Contributed `serenitydev.mcpToken` configuration property in `package.json`. [Done]

[DONE] Dynamic MCP Resource Registration:
- Added `resources/list` and `resources/read` handlers to `/mcp` router in `serenitydevserver.py`.
- Exposed `workspace://files` (JSON file tree) and `workspace://git_status` (working tree diff/status) for real-time diffs and file structures to connected MCP clients. [Done]

[ACTIVE] Overhaul Repeat Handling / Tool Call Loop Detection:
- Implement repeat detection toggle `[hyper, lazy, off]` so repetitive search/tool patterns (e.g. multi-query search, file discovery) proceed without false duplicate interception.

[ACTIVE] Model Roles & Agent Architecture:
- Explicitly assign `Agent` role to newly detected/selected models, reserving `Worker` role strictly for Supervisor / Orchestrator delegation.
- Build / refine new Orchestrator mode ("MiniBoss") with lightweight routing.

[ACTIVE] Streaming & Prompt Queue Optimization:
- Verify `generate_completion_stream` yields real-time streaming chunks across worker drafts and refinement.
- Implement workspace-specific buckets for prompt queue decongestion and stale request clearing.
- Add auto-parameter adjustment mode in `serenitydevserver.py` (temporary dynamic temperature/sampler adjustment per task type without overriding base user persona).

[ACTIVE] Model Parameter Tuning & Evaluation:
- Benchmark Gemma-4 coding performance, context retention, and parameter configurations (`temperature`, `top_p`, `min_p`, sampling headroom) to eliminate response skipping.
- Review FIM handling and dynamic offloading under high VRAM saturation.

[ACTIVE] Project Maintenance & Releases:
- Fix VS Code extension icon asset packaging.
- Add git commands / workspace actions to integration harness.
- [DONE] Update `README.md` and verify `LICENSE.md`.
Alow the orchestrator and supervisors to write tools if needed, and always ask user for review.

=========================================
NOTE ITEMS & EVOLVING CONSIDERATIONS
=========================================

N. Subprocess & Memory Safety:
- Ensure all subprocess backends respect localized environment variables and paths.
- During VRAM-intensive tests, use decoupled test runners that re-wake server/models post-test.

N. Supervisor Reasoning Profiles:
- High Profile (gemma-4-26B-A4B): Full thinking depth, exhaustive self-optimizing pipeline (research -> plan -> worker selection -> execution -> test).
- Low Profile (gemma-4-26B-A4B): Terse, token-optimized execution pipeline.
